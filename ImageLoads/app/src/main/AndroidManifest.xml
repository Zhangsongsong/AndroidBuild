<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">


    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <application
        android:name=".MApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher"
        android:theme="@style/Theme.ImageLoads">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity android:name=".activity.PersonListActivity" />
        <activity
            android:name=".activity.PersonDetailActivity"
            android:screenOrientation="portrait" />

        <activity android:name=".ui.xiuren.activity.XiuRenActivity" />
        <activity android:name=".ui.xiuren.activity.XiuRenWebViewActivity" />
        <activity android:name=".ui.common.CommonDownloadedActivity" />
        <activity android:name=".ui.common.SourceImageDetailActivity" />
        <activity android:name=".ui.meizi5.Meizi5Activity" />
        <activity android:name=".ui.taotu.TaoTuActivity" />
        <activity android:name=".ui.trendszine.TrendszineActivity" />

        <activity android:name=".ui.TestActivity" />


    </application>

</manifest>
